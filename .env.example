# Django API base URL (used by contact form; must be reachable from the browser)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Copy this file to .env.local and fill in your values:
# cp .env.example .env.local
